<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>${title} — ${request.registry['siteconfig']['site']['title']}</title>
    <link href="${request.static_path('static/style.css')}" rel="stylesheet">
</head>
<body>
    <div id="contentwrapper">
        <div id="content">
            <h1>${title}</h1>
            <tal:posts repeat="item items">
                <h1><a href="${item.url}">${item.title}</a></h1>
            </tal:posts>
        </div>
    </div>
</body>
</html>
